# الأشياء المشتركة وقابلة لإعادة الاستخدام في المشروع

تم فحص الدومينات الأساسية (خصوصًا الأكاديمي) لاكتشاف وتجميع العناصر المشتركة أو القابلة لإعادة الاستخدام بين أغلب الدومينات، بهدف توثيقها وتسهيل إعادة استخدامها أو تعميمها.

---

## 1. Enums مشتركة
- **Gender** (`Shared/Enums/Gender.php`): Enum موحد للجنس، مستخدم في الطلاب ومواضع أخرى.
- **DayOfWeek** (`Shared/Enums/DayOfWeek.php`): Enum موحد لأيام الأسبوع، مستخدم في الجداول الزمنية والتقارير.

## 2. خدمات مساعدة مشتركة
- **SharedLookupService** (`Shared/Services/SharedLookupService.php`): خدمة مركزية لجلب بيانات شائعة (الدول، أيام الأسبوع...) وتستخدم في أكثر من مكان.

## 3. Models مشتركة (Polymorphic)
- **Address** (`Shared/Models/Address.php`): موديل متعدد الأشكال يستخدم في أكثر من دومين (طالب، ولي أمر، ...).
- **Attachment** (`Shared/Models/Attachment.php`): موديل متعدد الأشكال للملفات والمرفقات.

## 4. Traits/سكوبات عامة
- **StudentScopes**: سكوبات متخصصة للطلاب، ويمكن تعميم بعض السكوبات (مثل byGender, active) كـ Trait عامة في Shared.
- **Traits بنية تحتية**: مثل HandlesSafeDelete, HasModelLabels, InvalidatesCache, HasFileDeletion، مستخدمة في عدة موديلات.

## 5. Data Objects
- توجد Data objects متكررة (مثل StudentData, StudentRegistrationData) يمكن توحيد بعض خصائصها أو جعلها ترث من Data object مشترك.

## 6. Helpers
- **StatsHelper** (`Shared/Services/StatsHelper.php`): هيلبر عام لحساب النسب ومستوى الإشغال، لكنه غير مستخدم فعليًا.

---

## التوصيات
- توثيق جميع Enums المشتركة في ملف توثيقي منفصل مع أمثلة الاستعمال.
- دراسة تعميم بعض السكوبات (byGender, active) كـ Trait عامة في Shared.
- توحيد استعمال Models المشتركة (Address, Attachment) وتوثيقها كـ Polymorphic pattern.
- مراجعة Data objects المتكررة وتوحيدها أو جعلها ترث من قاعدة بيانات مشتركة.
- مراجعة جميع الخدمات والهيلبرز المشتركة والتأكد من استخدامها أو حذفها إذا كانت غير مستخدمة.

---

> هذا الملف مرجعي لأي عملية Refactor أو تحسين معماري تخص العناصر المشتركة وقابلة لإعادة الاستخدام.
