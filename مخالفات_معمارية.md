# تقرير المخالفات المعمارية في المشروع

هذا الملف يوثق جميع المخالفات المعمارية المكتشفة في المشروع (school-dashboard) بناءً على الفحص الشامل للأكواد، مع تحديد نوع كل مخالفة، مكانها، وتوصية الإصلاح.

---

## 1. مخالفات الـ Enum (Magic Strings)

### أ. تجاوز الـ Enums الموجودة (Inconsistent Enum Usage)
- **الوصف:** تم إنشاء Enums لمعظم الحالات، ولكن الكود لا يزال يستخدم "نصوص سحرية" (Magic Strings) في أماكن حساسة بدلاً من الاعتماد على الـ Enum.
- **الحالة:** ⚠️ **Enum موجود لكن غير مستخدم**

#### 1. الدومين المالي (Finance)
- **Enum المتوفر:** `App\Domains\Finance\Enums\InvoiceStatus` 
- **القيم الموجودة:** `unpaid`, `paid`, `cancelled`
- **أماكن المخالفة (يتم استخدام String بدلاً من Enum):**
  - `app/Domains/Finance/Services/InvoiceService.php` (مقارنة مباشرة: `$invoice->status !== 'paid'`)
  - `app/Domains/Finance/Actions/CancelInvoiceAction.php`
  - `app/Domains/Finance/Actions/CreateInvoiceAction.php`
  - `app/Domains/Finance/Services/FinanceLookupService.php`

#### 2. الموارد البشرية (HR - Staff)
- **Enum المتوفر:** `App\Domains\HR\Staff\Enums\StaffStatus`
- **القيم الموجودة:** `active`, `terminated`, `resigned`
- **أماكن المخالفة:**
  - `app/Domains/HR/Staff/Models/Staff.php` (في الـ Scopes: `where('status', 'active')`)
  - `app/Domains/HR/Teacher/Services/TeacherLookupService.php`
  - `app/Domains/HR/Payroll/Models/Contract.php` (مقارنة: `where('status', 'active')`)

#### 3. الرواتب (HR - Payroll)
- **Enum المتوفر:** `App\Domains\HR\Payroll\Enums\PayrollBatchStatus` & `LoanStatus`
- **القيم الموجودة:** `draft`, `frozen`, `approved`, `paid`
- **أماكن المخالفة:**
  - `app/Domains/HR/Payroll/Actions/GeneratePayrollAction.php`
  - `app/Domains/HR/Payroll/Actions/ApprovePayrollAction.php`
  - `app/Domains/HR/Payroll/Services/PayrollCalculationService.php`

#### 4. النتائج والكنترول (Academic Results)
- **Enum المتوفر:** `App\Domains\Academic\Results\Enums\FinalResultStatus` & `ResultDecision`
- **القيم الموجودة:** `pass`, `fail`, `absent`
- **أماكن المخالفة:**
  - `app/Domains/Academic/Control/Services/FinalResultsViewerService.php` (استخدام مكثف للنصوص في الفلترة والحساب)
  - `app/Domains/Academic/Promotion/Services/AnnualResultService.php` (استخدام في جمل SQL Raw: `CASE WHEN decision = 'pass'`)
  - `app/Domains/Academic/Promotion/Services/PromotionService.php`

---

### ب. توصية الإصلاح (Action Plan)
1. **Refactor:** استبدال جميع النصوص المباشرة بـ `Enum::Case`.
   - *مثال:* بدلاً من `'paid'` استخدم `InvoiceStatus::Paid->value` (أو فقط `InvoiceStatus::Paid` إذا كان الحقل casted).
2. **Type Casting:** إضافة `casts` في الموديلات لضمان التحويل التلقائي:
   ```php
   protected $casts = [
       'status' => InvoiceStatus::class,
   ];
   ```
   (ملاحظة: بعض الموديلات مثل `Staff` لديها Cast، لكن الاستعلامات `where` ما زالت تستخدم String وهذا خطأ شائع).

---

## 2. تسرب منطق العمل (Business Logic Leaks)

### أ. مكونات Livewire "السمينة" (Fat Components)
- **الوصف:** وجود منطق عمل معقد، استعلامات قاعدة بيانات مباشرة، وعمليات إنشاء/تحديث داخل مكونات Livewire بدلاً من استخدام Actions أو Services.
- **الحالة:** ⚠️ **انتهاك واضح لمبدأ فصل المسؤوليات**

#### 1. جدول الحصص (TimetableBuilder) - 🛑 انتهاك جسيم
- **الملف:** `app/Livewire/Academic/TimetableBuilder.php`
- **المخالفات:**
  - **استعلام SQL مباشر:** استخدام `DB::table('grade_timetable_template')` في السطر 116.
  - **منطق معقد:** يحتوي على منطق كشف التعارضات (`conflict detection`) داخل المكون.
  - **كتابة مباشرة:** يقوم بـ `CourseOffering::updateOrCreate` و `Timetable::updateOrCreate` داخل دالة `saveSession`.

#### 2. إدارة القوالب (TimetableTemplateManager) - ⚠️ انتهاك طفيف
- **الملف:** `app/Livewire/Timetable/TimetableTemplateManager.php`
- **المخالفات:**
  - **استعلام SQL مباشر:** استخدام `\DB::table(...)` في دالة `assignedGradeIds` (سطر 146).
  - *ملاحظة إيجابية:* هذا المكون يستخدم Actions (`CreateTimetableTemplateAction`) للحفظ، مما يجعله أفضل من غيره.

#### 3. عمليات إنشاء مباشرة (Direct Model Creation)
- **الوصف:** مكونات تقوم بإنشاء السجلات مباشرة بدلاً من تفويض الأمر لـ Actions.
- **أماكن المخالفة:**
  - `app/Livewire/HR/WorkShiftManager.php`: يستدعي `WorkShift::create($data)` مباشرة.
  - `app/Livewire/Admin/Control/ControlDashboard.php`: يستدعي `ExamSession::create(...)`.
  - `app/Livewire/Admin/Access/RoleEditor.php`: يستدعي `Role::create(...)`.
  - `app/Livewire/Payroll/LoanManager.php`: يستدعي `Loan::create(...)` و `LoanInstallment::create(...)` بشكل متداخل ومعقد.

---

### ب. توصية الإصلاح (Action Plan)
1. **TimetableBuilder:**
   - نقل منطق كشف التعارض إلى `TimetableService`.
   - نقل عمليات الحفظ إلى `AssignSessionAction`.
   - استبدال `DB::table` بـ Repository method أو Scope.
2. **General Refactor:**
   - إنشاء Actions بسيطة (Create/Update) لكل نموذج يتم التعامل معه في Livewire.
   - مثال: `CreateWorkShiftAction` بدلاً من الاستدعاء المباشر في `WorkShiftManager`.

---

## 3. مخالفات مصدر الحقيقة للسنة النشطة (Active Academic Year)

### أ. تمرير أو استنتاج السنة خارج الـ Context
- **الوصف:** يجب أن يكون المصدر الوحيد للسنة النشطة هو:
  - `school()->activeAcademicYear()` أو ما يعادله
- **نتيجة الفحص:**
  - لم يتم العثور على مخالفات صريحة حالياً (حسب grep)، ولكن يجب الحذر عند استخدام `AcademicYear::where('status', 'active')->first()` مباشرة بدلاً من السرفيس.
- **توصية:**
  - فرض هذه القاعدة في جميع PRs
  - إضافة ADR رسمي يوضح ذلك

---

## 4. توصيات عامة
- إضافة فحص تلقائي (CI) لاكتشاف الاستعمال النصي للقيم التي يجب أن تكون Enum.
- توثيق جميع Enums ومساراتها في ملف توثيقي.
- مراجعة جميع الخدمات التي تتعامل مع السنة النشطة والتأكد من اعتمادها على الـ Context فقط.
- البدء فوراً في Refactor لمكونات Livewire الكبيرة (TimetableBuilder) لتقليل التعقيد.
- إدراج جميع البنود أعلاه في خطة الإصلاح (PR6/PR8) وكتابة ADR رسمي.

---

> هذا الملف مرجعي لأي عملية إصلاح أو Refactor معماري لاحق.

---

## 5. أدلة الفحص (Technical Evidence)

تم جمع هذه النتائج بناءً على أوامر البحث التالية في قاعدة الكود:

### أ. أوامر البحث المستخدمة (Methodology)
```bash
# 1. البحث عن النصوص السحرية (Magic Strings)
grep -rE "['\"](pending|approved|rejected|active|inactive|draft|published|closed|paid|unpaid|void|cash|card)['\"]" app/Domains

# 2. البحث عن تسرب المنطق في Livewire (Direct DB & Model Calls)
grep -rE "(::create\(|::update\(|->save\(|->delete\(|::updateOrCreate\()" app/Livewire
```

### ب. تفاصيل الملفات المخالفة (Detailed Findings)

#### 1. Magic Strings Violations
| القيمة (Value) | الملفات المتأثرة (Sample Files) |
| :--- | :--- |
| `pending`, `approved`, `rejected` | `HR/Leave/Models/LeaveRequest.php`, `HR/Leave/Services/LeaveService.php` |
| `draft`, `frozen`, `approved`, `paid` | `HR/Payroll/Models/PayrollBatch.php` |
| `setup`, `active`, `processing`, `published` | `Academic/Control/Models/ExamSession.php` |
| `paid`, `partially_paid` | `Finance/Actions/RecordPaymentAction.php` |
| `active`, `terminated` | `HR/Staff/Models/Staff.php` |

#### 2. Livewire Logic Leaks
| المكون (Component) | المخالفة (Violation) |
| :--- | :--- |
| `Academic/TimetableBuilder.php` | `updateOrCreate`, `delete` (Direct Logic) |
| `HR/WorkShiftManager.php` | `WorkShift::create`, `update`, `delete` (Direct Logic) |
| `Payroll/PayrollBatchManager.php` | `$batch->delete()` (Direct Logic - Minor) |
| `Admin/Access/RoleManager.php` | Direct Model Manipulation |
| `Guardian/GuardianManager.php` | Direct Model Manipulation |

